/**
 * @timesheets/test-fixtures
 *
 * Golden fixture data for parity testing between the PHP oracle and the TypeScript engine.
 * Populate these fixtures as part of Phase 0 (Freeze the Contract) by capturing real
 * output from the PHP implementation before removing it.
 *
 * Each fixture set should cover:
 *   - one sample day report
 *   - one multi-day range report
 *   - one filtered (by project/grouping) report
 *   - one config round-trip
 */

/** @type {Record<string, unknown>} */
export const fixtures = {};
